# Google Calendar MCP Server Environment Variables

# Default calendar ID to use when not specified in tool calls
# Defaults to 'primary' if not set
# You can find your calendar IDs by running the list-calendars tool
# Examples:
# - primary (default personal calendar)
# - work@example.com (specific calendar by email)
# - abc123@group.calendar.google.com (shared calendar ID)
GOOGLE_CALENDAR_ID=primary

# Node.js binary path (optional)
# Use this if Claude Desktop uses an incompatible Node.js version
# Must be Node.js 18 or higher
# Leave commented to use system Node.js
# Examples:
# NODE_PATH=/usr/local/bin/node
# NODE_PATH=/Users/username/.nvm/versions/node/v22.11.0/bin/node
# NODE_PATH=C:\Program Files\nodejs\node.exe
# NODE_PATH=